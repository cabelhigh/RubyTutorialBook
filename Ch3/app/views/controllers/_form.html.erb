<%= form_for(controller) do |f| %>
  <% if controller.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(controller.errors.count, "error") %> prohibited this controller from being saved:</h2>

      <ul>
      <% controller.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :StaticPages %>
    <%= f.text_field :StaticPages %>
  </div>

  <div class="field">
    <%= f.label :home %>
    <%= f.text_field :home %>
  </div>

  <div class="field">
    <%= f.label :help %>
    <%= f.text_field :help %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
